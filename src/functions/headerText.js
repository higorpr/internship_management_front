export default function headerText(input) {
    switch (input) {
        case '/':
            return 'Plataforma de Controle de Relatórios de Estágio'             
        default:
            break;
    }
}